<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>com.nsn.cumulocity.dependencies</groupId>
	<artifactId>cumulocity-dependencies</artifactId>
	<version>5.24.0-SNAPSHOT</version>
	<packaging>pom</packaging>
	<name>Cumulocity Dependencies</name>

	<organization>
		<name>Cumulocity GmbH</name>
		<url>https://cumulocity.com</url>
	</organization>

	<scm>
		<connection>scm:hg:https://bitbucket.org/m2m/cumulocity-dependencies</connection>
	</scm>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<maven.compiler.source>1.6</maven.compiler.source>
		<maven.compiler.target>1.6</maven.compiler.target>

		<osgi.service.http.port>8181</osgi.service.http.port>
		<nexus.port>443</nexus.port>

		<activation.version>1.1</activation.version>
		<osgi.activation.version>${activation.version}-${project.parent.version}</osgi.activation.version>
		<antlr.version>2.7.7</antlr.version>
		<osgi.antlr.version>${antlr.version}-${project.parent.version}</osgi.antlr.version>
		<antlr-runtime.version>3.2</antlr-runtime.version>
		<osgi.antlr-runtime.version>${antlr-runtime.version}-${project.parent.version}</osgi.antlr-runtime.version>
		<apache-log4j-extras.version>1.1</apache-log4j-extras.version>
		<osgi.apache-log4j-extras.version>${apache-log4j-extras.version}-${project.parent.version}</osgi.apache-log4j-extras.version>
		<asm.version>3.1</asm.version>
		<osgi.asm.version>${asm.version}-${project.parent.version}</osgi.asm.version>
		<osgi.aspectj.version>${aspectj.version}-${project.parent.version}</osgi.aspectj.version>
		<cglib-nodep.version>2.2</cglib-nodep.version>
		<osgi.cglib-nodep.version>${cglib-nodep.version}-${project.parent.version}</osgi.cglib-nodep.version>
		<collections-generic.version>4.01</collections-generic.version>
		<osgi.collections-generic.version>${collections-generic.version}-${project.parent.version}</osgi.collections-generic.version>
		<commons-codec.version>1.7</commons-codec.version>
		<osgi.commons-codec.version>${commons-codec.version}-${project.parent.version}</osgi.commons-codec.version>
		<commons-configuration.version>1.7</commons-configuration.version>
		<osgi.commons-configuration.version>${commons-configuration.version}-${project.parent.version}</osgi.commons-configuration.version>
		<commons-httpclient.version>3.1</commons-httpclient.version>
		<osgi.commons-httpclient.version>${commons-httpclient.version}-${project.parent.version}</osgi.commons-httpclient.version>
		<dozer.version>5.4.0</dozer.version>
		<osgi.dozer.version>${dozer.version}-${project.parent.version}</osgi.dozer.version>
		<ektorp.version>1.2.2</ektorp.version>
		<osgi.ektorp.version>${ektorp.version}-${project.parent.version}</osgi.ektorp.version>
		<esper.version>4.10.0.07102013</esper.version>
		<osgi.esper.version>${esper.version}-${project.parent.version}</osgi.esper.version>
		<findbugs.version>1.3.2</findbugs.version>
		<osgi.findbugs.version>${findbugs.version}-${project.parent.version}</osgi.findbugs.version>
		<geronimo-annotation.version>1.1.1</geronimo-annotation.version>
		<osgi.geronimo-annotation.version>${geronimo-annotation.version}-${project.parent.version}</osgi.geronimo-annotation.version>
		<guava.version>13.0.1</guava.version>
		<osgi.guava.version>${guava.version}-${project.parent.version}</osgi.guava.version>
		<httpclient.version>4.1.2</httpclient.version>
		<osgi.httpclient.version>${httpclient.version}-${project.parent.version}</osgi.httpclient.version>
		<httpcore.version>4.1.4</httpcore.version>
		<osgi.httpcore.version>${httpcore.version}-${project.parent.version}</osgi.httpcore.version>
		<inject.version>1</inject.version>
		<osgi.inject.version>${inject.version}-${project.parent.version}</osgi.inject.version>
		<jaxb-api.version>2.1</jaxb-api.version>
		<osgi.jaxb-api.version>${jaxb-api.version}-${project.parent.version}</osgi.jaxb-api.version>
		<jcouchdb.version>1.0.1-1</jcouchdb.version>
		<osgi.jcouchdb.version>${jcouchdb.version}-${project.parent.version}</osgi.jcouchdb.version>
		<jersey.version>1.8</jersey.version>
		<osgi.jersey.version>${jersey.version}-${project.parent.version}</osgi.jersey.version>
		<liquibase.version>1.9.5</liquibase.version>
		<osgi.liquibase.version>${liquibase.version}-${project.parent.version}</osgi.liquibase.version>
		<mail.version>1.4</mail.version>
		<osgi.mail.version>${mail.version}-${project.parent.version}</osgi.mail.version>
		<mimepull.version>1.4</mimepull.version>
		<osgi.mimepull.version>${mimepull.version}-${project.parent.version}</osgi.mimepull.version>
		<mysema-codegen.version>0.4.9</mysema-codegen.version>
		<osgi.mysema-codegen.version>${mysema-codegen.version}-${project.parent.version}</osgi.mysema-codegen.version>
		<mysema-commons.version>0.2.3</mysema-commons.version>
		<osgi.mysema-commons.version>${mysema-commons.version}-${project.parent.version}</osgi.mysema-commons.version>
		<postgresql.version>9.1-901-1.jdbc4</postgresql.version>
		<osgi.postgresql.version>${postgresql.version}-${project.parent.version}</osgi.postgresql.version>
		<querydsl.version>2.7.3</querydsl.version>
		<osgi.querydsl.version>${querydsl.version}-${project.parent.version}</osgi.querydsl.version>
		<spring.security.jwt.version>1.0.0.M3</spring.security.jwt.version>
		<osgi.spring.security.jwt.version>${spring.security.jwt.version}-${project.parent.version}</osgi.spring.security.jwt.version>
		<stringtemplate.version>3.2</stringtemplate.version>
		<osgi.stringtemplate.version>${stringtemplate.version}-${project.parent.version}</osgi.stringtemplate.version>
		<stax-api.version>1.0-2</stax-api.version>
		<osgi.stax-api.version>${stax-api.version}-${project.parent.version}</osgi.stax-api.version>
		<svenson.version>1.3.8</svenson.version>
		<osgi.svenson.version>${svenson.version}-${project.parent.version}</osgi.svenson.version>
		<json-path.version>0.8.1</json-path.version>
		<osgi.json-path.version>${json-path.version}-${project.parent.version}</osgi.json-path.version>
		<json-smart.version>1.2</json-smart.version>
		<osgi.json-smart.version>${json-smart.version}-${project.parent.version}</osgi.json-smart.version>
		<super-csv.version>2.1.0</super-csv.version>
		<osgi.super-csv.version>${super-csv.version}-${project.parent.version}</osgi.super-csv.version>
		<cometd.version>2.8.0</cometd.version>
		<osgi.cometd.version>${cometd.version}-${project.parent.version}</osgi.cometd.version>

		<joda-time.version>1.6.2</joda-time.version>
		<commons-io.version>2.0.1</commons-io.version>
		<cxf.version>2.6.7</cxf.version>
		<aspectj.version>1.6.12</aspectj.version>
		<eclipselink.version>2.2.1</eclipselink.version>
		<ehcache.version>2.5.1.1</ehcache.version>
		<jetty.version>7.6.8.v20121106</jetty.version>
		<karaf.version>2.3.1</karaf.version>
		<pax.logging.version>1.7.0</pax.logging.version>
		<pax.url.version>1.3.5</pax.url.version>
		<pax.web.version>1.1.12</pax.web.version>
		<postgresql.version>9.1-901-1.jdbc4</postgresql.version>
		<slf4j.version>1.7.0</slf4j.version>
		<spring.version>3.1.4.RELEASE</spring.version>
		<spring.osgi.version>1.2.1</spring.osgi.version>

		<hamcrest.version>1.1</hamcrest.version>
		<junit.version>4.8.2</junit.version>
		<fest-assert.version>1.4</fest-assert.version>
		<mockito.version>1.8.5</mockito.version>
		<powermock.version>1.4.9</powermock.version>
	</properties>

	<modules>
		<module>cumulocity-sdk-resource-bundles</module>
		<module>kpi-logger</module>
		<module>osgi-dependencies</module>
		<module>osgi-fragments</module>
		<module>osgi-features</module>
		<module>cumulocity-base-karaf</module>
	</modules>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>javax.servlet</groupId>
				<artifactId>servlet-api</artifactId>
				<version>2.5</version>
			</dependency>
			<dependency>
				<groupId>joda-time</groupId>
				<artifactId>joda-time</artifactId>
				<version>${joda-time.version}</version>
			</dependency>
			<dependency>
				<groupId>junit</groupId>
				<artifactId>junit</artifactId>
				<version>${junit.version}</version>
				<scope>test</scope>
			</dependency>
			<dependency>
				<groupId>org.aspectj</groupId>
				<artifactId>aspectjrt</artifactId>
				<version>${aspectj.version}</version>
			</dependency>
			<dependency>
				<groupId>org.easytesting</groupId>
				<artifactId>fest-assert</artifactId>
				<version>${fest-assert.version}</version>
				<scope>test</scope>
			</dependency>
			<dependency>
				<groupId>org.hamcrest</groupId>
				<artifactId>hamcrest-all</artifactId>
				<version>${hamcrest.version}</version>
				<scope>test</scope>
			</dependency>
			<dependency>
				<groupId>org.mockito</groupId>
				<artifactId>mockito-all</artifactId>
				<version>${mockito.version}</version>
				<scope>test</scope>
			</dependency>
			<dependency>
				<groupId>org.powermock</groupId>
				<artifactId>powermock-api-mockito</artifactId>
				<version>${powermock.version}</version>
				<scope>test</scope>
			</dependency>
			<dependency>
				<groupId>org.powermock</groupId>
				<artifactId>powermock-module-junit4</artifactId>
				<version>${powermock.version}</version>
				<scope>test</scope>
			</dependency>
			<dependency>
				<groupId>org.slf4j</groupId>
				<artifactId>jcl-over-slf4j</artifactId>
				<version>${slf4j.version}</version>
			</dependency>
			<dependency>
				<groupId>org.slf4j</groupId>
				<artifactId>slf4j-api</artifactId>
				<version>${slf4j.version}</version>
			</dependency>
			<dependency>
				<groupId>org.slf4j</groupId>
				<artifactId>slf4j-log4j12</artifactId>
				<version>${slf4j.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springframework</groupId>
				<artifactId>spring-aop</artifactId>
				<version>${spring.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springframework</groupId>
				<artifactId>spring-asm</artifactId>
				<version>${spring.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springframework</groupId>
				<artifactId>spring-beans</artifactId>
				<version>${spring.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springframework</groupId>
				<artifactId>spring-context</artifactId>
				<version>${spring.version}</version>
			</dependency>
			<dependency>
				<groupId>org.cometd.java</groupId>
				<artifactId>cometd-java-server</artifactId>
				<version>${cometd.version}</version>
			</dependency>
			<dependency>
				<groupId>org.cometd.java</groupId>
				<artifactId>cometd-java-oort</artifactId>
				<version>${cometd.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springframework</groupId>
				<artifactId>spring-core</artifactId>
				<version>${spring.version}</version>
				<exclusions>
					<!-- Exclude Commons Logging in favor of SLF4j -->
					<exclusion>
						<groupId>commons-logging</groupId>
						<artifactId>commons-logging</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>org.springframework</groupId>
				<artifactId>spring-expression</artifactId>
				<version>${spring.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springframework</groupId>
				<artifactId>spring-web</artifactId>
				<version>${spring.version}</version>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<build>
		<extensions>
			<!-- Enabling the use of sftp -->
			<extension>
				<groupId>org.apache.maven.wagon</groupId>
				<artifactId>wagon-ssh-external</artifactId>
				<version>1.0-beta-6</version>
			</extension>
		</extensions>

		<plugins>
			<plugin>
				<groupId>org.apache.felix</groupId>
				<artifactId>maven-bundle-plugin</artifactId>
			</plugin>
		</plugins>

		<pluginManagement>
			<plugins>
				<plugin>
					<groupId>org.apache.felix</groupId>
					<artifactId>maven-bundle-plugin</artifactId>
					<version>2.3.5</version>
					<configuration>
						<instructions>
							<_nouses>true</_nouses>
						</instructions>
					</configuration>
					<executions>
						<execution>
							<id>bundle-manifest</id>
							<phase>process-classes</phase>
							<goals>
								<goal>manifest</goal>
							</goals>
						</execution>
					</executions>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-dependency-plugin</artifactId>
					<version>2.6</version>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-compiler-plugin</artifactId>
					<version>3.0</version>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-jar-plugin</artifactId>
					<version>2.3.2</version>
					<configuration>
						<archive>
							<manifestFile>${project.build.outputDirectory}/META-INF/MANIFEST.MF</manifestFile>
						</archive>
					</configuration>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-resources-plugin</artifactId>
					<version>2.6</version>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-source-plugin</artifactId>
					<version>2.2.1</version>
				</plugin>
				<plugin>
					<groupId>org.codehaus.mojo</groupId>
					<artifactId>aspectj-maven-plugin</artifactId>
					<version>1.4</version>
					<executions>
						<execution>
							<id>kpi-weaver</id>
							<phase>process-sources</phase>
							<goals>
								<goal>compile</goal>
							</goals>
							<configuration>
								<complianceLevel>1.6</complianceLevel>
								<showWeaveInfo>true</showWeaveInfo>
								<verbose>true</verbose>
							</configuration>
						</execution>
					</executions>
					<dependencies>
						<dependency>
							<groupId>org.aspectj</groupId>
							<artifactId>aspectjtools</artifactId>
							<version>${aspectj.version}</version>
						</dependency>
					</dependencies>
				</plugin>
				<plugin>
					<groupId>org.codehaus.mojo</groupId>
					<artifactId>build-helper-maven-plugin</artifactId>
					<version>1.7</version>
				</plugin>
				<!--This plugin's configuration is used to store Eclipse m2e settings 
					only. It has no influence on the Maven build itself. -->
				<plugin>
					<groupId>org.eclipse.m2e</groupId>
					<artifactId>lifecycle-mapping</artifactId>
					<version>1.0.0</version>
					<configuration>
						<lifecycleMappingMetadata>
							<pluginExecutions>
								<pluginExecution>
									<pluginExecutionFilter>
										<groupId>
											org.apache.maven.plugins
										</groupId>
										<artifactId>
											maven-dependency-plugin
										</artifactId>
										<versionRange>
											[2.2,)
										</versionRange>
										<goals>
											<goal>copy</goal>
											<goal>copy-dependencies</goal>
											<goal>unpack</goal>
											<goal>unpack-dependencies</goal>
										</goals>
									</pluginExecutionFilter>
									<action>
										<ignore />
									</action>
								</pluginExecution>
							</pluginExecutions>
						</lifecycleMappingMetadata>
					</configuration>
				</plugin>
			</plugins>
		</pluginManagement>
	</build>

	<distributionManagement>
		<snapshotRepository>
			<id>snapshot</id>
			<url>http://localhost:8080/nexus/content/repositories/snapshots/</url>
		</snapshotRepository>
		<repository>
			<id>release</id>
			<url>http://localhost:8080/nexus/content/repositories/releases/</url>
		</repository>
	</distributionManagement>
	<profiles>
		<profile>
			<id>publicDeployment</id>
			<distributionManagement>
				<snapshotRepository>
					<id>resources-snapshot</id>
					<url>scpexe://resources.cumulocity.com/cumulocity/maven/repository-snapshots</url>
				</snapshotRepository>
				<repository>
					<id>resources-release</id>
					<url>scpexe://resources.cumulocity.com/cumulocity/maven/repository</url>
				</repository>
			</distributionManagement>
		</profile>
	</profiles>

</project>
